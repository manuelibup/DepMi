'use client';

export default function WalletClient() {
    return (
        <div style={{ padding: '40px 24px', textAlign: 'center', color: 'var(--text-muted)' }}>
            <p>Crypto wallet features are coming soon.</p>
        </div>
    );
}
