export const metadata = { title: 'Crypto Wallet — DepMi' };

export default function StoreWalletPage() {
    return (
        <main style={{ maxWidth: 560, margin: '0 auto', padding: '48px 16px', textAlign: 'center', color: 'var(--text-muted)' }}>
            <p>Crypto wallet features are coming soon.</p>
        </main>
    );
}
